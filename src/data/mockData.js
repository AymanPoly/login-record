import { tokens } from "../theme";
import data from './mockDataTeam.json';
import Contacts from './Contancts.json';
import inovoices from './mockDataInvoices.json';
import mockTrans from './mockTransactions.json';
import MockBar from './mockBarData.json';
import Pie from './mockPieData.json';
import mockLine from './mockLineData.json';
import Geo from './mockGeographyData.json';
export const mockDataTeam =  JSON.parse(data);

export const mockDataContacts = JSON.parse(Contacts);

export const mockTransactions = JSON.parse(mockTrans);

export const mockDataInvoices =JSON.parse(inovoices);

export const mockBarData = JSON.parse(MockBar);

export const mockPieData = JSON.parse(Pie);

export const mockLineData = JSON.parse(mockLine);

export const mockGeographyData = JSON.parse(Geo);